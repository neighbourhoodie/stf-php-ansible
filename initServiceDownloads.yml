# Run this playbook to set up your downloads machine
# ansible-playbook inventory/php initServiceDownloads.yml"

- hosts:
  - downloads

  become: yes
  become_user: root

  roles:
    - role: properties/downloads
    - role: properties/shared
