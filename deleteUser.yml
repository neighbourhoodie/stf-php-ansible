- hosts:
  - all

  # remote_user: root
  become: yes
  become_user: root
  # become_method: su

  pre_tasks:
    - name: Hello user
      debug:
        msg: "Hello {{ local_user }}"
  tasks:
  - name: Delete an existing linux user
    user:
      name: "{{ username }}"
      state: absent
      force: true
      remove: yes

  # roles:
  # - role: delete_user
